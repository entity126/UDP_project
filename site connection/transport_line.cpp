#include "transport_line.h"

int main() {
    transport t;
    UDP_packet packet1, packet2, packet3;

    t.receive_packets(packet1, packet2, packet3);

    return 0;
}

int main() {
    transport t;
    UDP_packet packet1, packet2, packet3;

    t.receive_packets(packet1, packet2, packet3);

    return 0;
}
